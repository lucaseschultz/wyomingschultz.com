Remove transitions from “Who Am I”, for mobile

Reduce “Skills” animation zone to less that screen

remove unused images

revisit skills list

replace open AI icon with generic

Let components get more on page, before activating appear animation

audit styles.scss: refactor w3 classes (make unique class names)

audit mobile styling

add these mixins?
@mixin card-container($background-color: var(--foreground), $border-radius: 8px, $border: 1px solid var(--outline),
                      $padding: 2rem) {
  background-color: $background-color;
  border-radius: $border-radius;
  border: $border;
  padding: $padding;
}
@mixin button-style($padding: 0.75rem 1.5rem, $bg-color: var(--interactive-color), $text-color: white) {
  display: inline-block;
  padding: $padding;
  background-color: $bg-color;
  color: $text-color;
  border: none;
  border-radius: 4px;
  font-size: 1rem;
  font-weight: 500;
  cursor: pointer;
  text-decoration: none;

  &:hover {
    background-color: var(--faded-interactive-color);
    transform: scale(1.05);
  }

  &:active {
    transform: scale(1);
  }
}

fix mobile profile image display being uncentered
